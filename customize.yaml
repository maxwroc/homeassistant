sensor.energy_daily:
  friendly_name: Energy today


sensor.files_changed_locally:
  icon: mdi:file-star
  friendly_name: "Files changed locally"

sensor.config_last_backup:
  icon: mdi:cloud-upload
  friendly_name: "Config last backup"

sensor.pc_status:
  templates:
    rgb_color: "if (state === 'On') return [251, 210, 41]; else return [54, 95, 140];"



sensor.changes_not_committed:
  templates:
    rgb_color: "if (state === '0') return [0, 255, 0]; else return [255, 0, 0];"